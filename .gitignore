# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/
dist/
build/

# Downloaded speech models (kokoro onnx + voices, multi-hundred-MB)
backend/.models/

# Session history persisted between runs (Phase 2.5)
.jarvis_history.json

# Append-only record of every tool call (Phase 3)
.jarvis_audit.jsonl

# Flutter / Dart
.dart_tool/
.flutter-plugins*
.pub-cache/
build/

# IDE
.idea/
.vscode/

# Env & secrets (per plan §2.5: secrets go in Credential Manager, not files)
.env
.aider*
